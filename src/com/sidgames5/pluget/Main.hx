package com.sidgames5.pluget;

class Main {
	public static function main() {
		final args = Sys.args();
		switch (args[0]) {
			case "instance":
				switch (args[1]) {
					case "add":
						final name = args[2];
						final version = args[3];
						final path = args[4];
				}
			case "get":
			case "remove":
			case "update":
		}
	}
}
